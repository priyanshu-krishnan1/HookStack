{
  "name": "webhook-template-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Reusable webhook template with sender, receiver, worker, and tests",
  "scripts": {
    "test": "jest --runInBand",
    "test:unit": "jest --runInBand tests/unit",
    "test:integration": "jest --runInBand tests/integration",
    "test:architecture": "jest --runInBand tests/e2e",
    "test:live": "jest --runInBand tests/live"
  },
  "devDependencies": {
    "amqplib": "^1.0.3",
    "jest": "^30.3.0",
    "pg": "^8.13.1",
    "supertest": "^7.0.0"
  }
}